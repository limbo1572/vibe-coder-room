# Vibe Coder Tycoon — Дизайн механіки (для Godot)

Айдл/клікер про вайб-кодинг. Двовалютна економіка (LoC → $) з механікою багів як антагоніст.

---

## 1. Ресурси (стан гри)

| Ресурс | Що це | Початкове значення |
|---|---|---|
| `loc` | Рядки коду — основна "сира" валюта | 0 |
| `money` | Долари — за них купуєш апгрейди | 0 |
| `bugs` | Накопичені баги — ріжуть продуктивність | 0 |
| `loc_per_click` | Скільки LoC за один тап | 1 |
| `loc_per_sec` | Пасивна генерація LoC/сек | 0 |
| `deploy_rate` | Курс конвертації LoC → $ при деплої | 0.10 ($ за 1 LoC) |
| `qa_power` | Скільки багів QA прибирає за секунду | 0 |
| `prestige_mult` | Множник від престижу ("рефакторинги") | 1.0 |
| `prestige_points` | Накопичені очки престижу | 0 |

---

## 2. Основні формули

### Клік
```
on_click():
    gain = loc_per_click * prestige_mult * productivity_factor()
    loc += gain
    bugs += gain * BUG_RATE_CLICK   # швидкий брудний код плодить баги
```
`BUG_RATE_CLICK = 0.05` (5% від набитого коду стає багами)

### Пасивна генерація (кожен tick / delta)
```
_process(delta):
    gain = loc_per_sec * prestige_mult * productivity_factor() * delta
    loc += gain
    bugs += gain * BUG_RATE_PASSIVE * delta
    bugs = max(0, bugs - qa_power * delta)   # QA чистить баги
```
`BUG_RATE_PASSIVE = 0.03`

### Productivity factor (вплив багів)
```
productivity_factor():
    # що більше багів — то нижча продуктивність, але не нижче 10%
    return clamp(1.0 - (bugs / (bugs + 100)), 0.1, 1.0)
```
Тобто 100 багів = 50% продуктивності, 300 багів = 25%, і т.д. М'яка крива, не вбиває різко.

### Деплой (конвертація LoC → $)
```
deploy():
    earned = loc * deploy_rate * prestige_mult
    money += earned
    loc = 0
    # деплой "чистого" коду не плодить баги, але можна додати ризик-варіант
```

---

## 3. Апгрейди (купуються за $)

Ціна росте експоненційно: `cost = base_cost * (1.15 ^ owned)`

### Генератори (пасивний LoC/сек)
| Назва | base_cost | +loc_per_sec | Мемний опис |
|---|---|---|---|
| Stack Overflow Premium | 15 | 0.2 | "Копіпаст без капчі" |
| ChatGPT Plus | 100 | 1 | "Він пише, ти кермуєш" |
| Молодший джун | 500 | 4 | "Дешевий, але треба ревʼюити" |
| Сіньйор з YouTube | 3000 | 20 | "Подивився туторіал — вже експерт" |
| Армія AI-агентів | 20000 | 120 | "Вони не сплять, вони деплоять" |

### Клік-апгрейди (+loc_per_click)
| Назва | base_cost | ефект | Опис |
|---|---|---|---|
| Механічна клава | 50 | +1 click | "Клац-клац дофамін" |
| Дві руки | 300 | x2 click | "Хто сказав що одна?" |
| Tab-tab-tab автокомпліт | 2000 | +5 click | "IntelliSense головного мозку" |

### QA (зменшує баги)
| Назва | base_cost | +qa_power | Опис |
|---|---|---|---|
| Лінтер | 200 | 0.5 | "Червоні підкреслення сорому" |
| Юніт-тести | 1500 | 3 | "Зелененькі галочки = дофамін" |
| QA-інженер | 10000 | 15 | "Той хто знаходить твій сором" |

### Деплой-апгрейди (підвищують deploy_rate)
| Назва | base_cost | deploy_rate | Опис |
|---|---|---|---|
| CI/CD пайплайн | 800 | +0.05 | "Деплой у пʼятницю? Сміливо" |
| Хмарний хостинг | 5000 | +0.15 | "Чужий компʼютер за твої гроші" |

---

## 4. Престиж ("Рефакторинг з нуля")

Доступний коли `money >= 1,000,000` (перший поріг).
```
prestige():
    new_points = floor(sqrt(money / 1_000_000))
    prestige_points += new_points
    prestige_mult = 1.0 + prestige_points * 0.1   # +10% за очко
    # ОБНУЛИТИ: loc, money, bugs, всі куплені апгрейди, loc_per_click=1, loc_per_sec=0, qa_power=0
    # ЗБЕРЕГТИ: prestige_points, prestige_mult
```
Мемний текст престижу: "Ти видалив усе і почав заново. Цього разу буде чисто. (Не буде.)"

---

## 5. UI (накладка поверх ізо-кімнати)

Не ламати сцену кімнати — UI це CanvasLayer зверху:
- **Зверху:** лічильники LoC, $, Bugs (з іконкою/кольором: баги червоніють коли productivity падає)
- **Центр-низ:** велика кнопка КЛІК ("git commit") — або клік прямо по кодеру/монітору в кімнаті
- **Кнопка DEPLOY** ("git push" — логічно, бо в нас вивіска!) — конвертує LoC у $
- **Бокова панель:** список апгрейдів (скрол), кожен з ціною, ефектом, мемним описом; сірі якщо не по кишені
- **Кнопка PRESTIGE** — зʼявляється тільки коли поріг досягнуто

### Візуальний звʼязок з кімнатою (приємні дрібниці, опційно)
- RGB-стрічка пульсує яскравіше коли висока продуктивність
- Екран монітора "блимає кодом" активніше при високому loc_per_sec
- Коли багів багато — екран монітора червоніє / зʼявляється "ERROR"

---

## 6. Збереження
Godot: зберігати стан у `user://save.json` — усі ресурси + куплені апгрейди (як словник назва→кількість). Автозбереження кожні 10 сек + при закритті. Завантаження при старті. Офлайн-прогрес (опційно, друга ітерація): порахувати скільки часу минуло, нарахувати loc_per_sec за цей час з кепом ~8 годин.

---

## 7. Порядок реалізації (щоб не зробити все й зламати)
1. Стан + клік + лічильник LoC
2. Deploy (LoC → $)
3. Один генератор-апгрейд (перевірити петлю покупки + пасив)
4. Решта генераторів + клік-апгрейди
5. Баги + productivity_factor
6. QA-апгрейди
7. Збереження
8. Престиж
9. Полір: мемні тексти, звʼязок з кімнатою, числа з форматуванням (1.2K, 3.4M)
